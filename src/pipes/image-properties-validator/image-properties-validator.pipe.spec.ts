import { ImagePropertiesValidatorPipe } from './image-properties-validator.pipe';

describe('ImagePropertiesValidatorPipe', () => {
  it('should be defined', () => {
    expect(new ImagePropertiesValidatorPipe()).toBeDefined();
  });
});
