import { ImagePropertiesTransformerPipe } from './image-properties-transformer.pipe';

describe('ImagePropertiesTransformerPipe', () => {
  it('should be defined', () => {
    expect(new ImagePropertiesTransformerPipe()).toBeDefined();
  });
});
