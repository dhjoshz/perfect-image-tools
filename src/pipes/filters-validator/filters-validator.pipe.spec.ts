import { FiltersValidatorPipe } from './filters-validator.pipe';

describe('FiltersValidatorPipe', () => {
  it('should be defined', () => {
    expect(new FiltersValidatorPipe()).toBeDefined();
  });
});
